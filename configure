#!/bin/sh

cat > config.mak <<EOF
CC=$1
VERBOSITY=0
CFLAGS_COMMON= --pedantic -fPIC
CFLAGS_DEBUG= -O0 -g
CFLAGS_RELEASE= -Werror -O3
SSE_CFLAGS= -mssse3
SHARED_LIBSUFF:=.so
STATIC_LIBSUFF:=.a
BUILDDIR=.
EOF

exit 0
